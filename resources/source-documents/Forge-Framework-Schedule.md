# Forge Framework Schedule
> Adapted from `Forge-Framework-Schedule.pdf` — see `resources/raw/`
>
> Blank schedule template. The PDF contains an empty table structure with no filled-in content.

## Structure

The template is a repeating block layout with **7 sections** (rows of blocks), each containing:
- A wide task/description column
- A date/duration column
- A status column

Each section has 3–4 line items plus a separator row.

This appears to be a generic project schedule template designed to track milestones and tasks across phases of a project. No data was filled in on this copy.
